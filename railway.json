{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'apt-get update && apt-get install -y --no-install-recommends libgl1 libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 libxrender1 && gunicorn api.main:app --bind 0.0.0.0:$PORT --workers 1'",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
